= page_header("Presentation Schedule")

%h5 Jessica Rowe ElderCare Consulting, LLC is authorized by the Board of Social Work Examiners in Maryland to sponsor Category I social work continuing education programs.

.row.content
  - if @events.any?
    - @events.each do |event|
      .event
        %h3.title= event.date_and_title
        %h4.time= event.time
        %p= event.notes
        .address
          = event.address
        = event.contact
        = event.rsvp
  - else
    %h3 Jessica has no currently scheduled events. Please check back for updates.
